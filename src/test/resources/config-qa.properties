# ===================================================
# API Framework Configuration - QA Environment
# ===================================================

# API Base URL (using free public APIs for demonstration)
api.base.url=https://jsonplaceholder.typicode.com
api.reqres.base.url=https://reqres.in/api

# Timeouts
api.connection.timeout=10000
api.response.timeout=30000

# Auth (example - reqres.in doesn't need real auth)
api.auth.type=bearer
api.auth.token=QpwL5tke4Pnpja7X4

# Database Configuration (PostgreSQL)
# NOTE: Update these with your actual DB credentials
db.url=jdbc:postgresql://localhost:5432/testdb
db.username=testuser
db.password=testpass
db.driver=org.postgresql.Driver

# Logging
log.request=true
log.response=true

# Allure
allure.results.directory=target/allure-results
